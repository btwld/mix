## Unreleased

- Add the strict `mix_atlas/component/v2` contract, typed producer builder,
  composite Mix style projector, embedded style libraries, generic bindings,
  semantics, nested components, and all portable renderer primitives.
- Retain ordered atlas axes, rows, scenarios, widget states, and properties and
  validate exact recipe/state alignment across every captured theme.
- Index and compare every catalog component and visual oracle while preserving
  component-v1 and legacy partial-capture compatibility.

## 0.1.0

- Extract validated Atlas capture loading and bounded portable rendering.
- Allow catalog components to remain rendered-only when no portable document
  is captured, and raise the bounded artifact count for full design systems.
