/// Pure Dart APIs for building and checking deterministic Atlas bundles.
///
/// Command-line packagers should import this entry point so they do not load
/// Flutter-dependent component projection and rendering libraries.
library;

export 'src/packaging/capture_packager.dart';
