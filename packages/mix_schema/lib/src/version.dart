/// Version of the Mix JSON Schema this implementation targets.
///
/// Mirrors the `schema` field in `MixJsonDocument`. Bumped per the
/// versioning rules in spec.md §Versioning.
library;

const String mixSchemaVersion = '1.0.0-draft';
