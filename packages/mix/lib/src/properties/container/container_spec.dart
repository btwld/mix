import '../../specs/box/box_spec.dart';

/// Legacy alias for ContainerSpec pointing to BoxSpec.
///
/// This typedef maintains backward compatibility for imports that still
/// reference `src/properties/container/container_spec.dart`.
typedef ContainerSpec = BoxSpec;

