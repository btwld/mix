name: rendering_pipeline
description: Reproducible Mix versus Flutter rendering-pipeline benchmarks.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  mix:
    path: ../../packages/mix
  vm_service: ^15.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
